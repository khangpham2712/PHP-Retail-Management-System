import { makeStyles } from "@material-ui/core";
export default makeStyles((theme) => ({
  margin: {
    marginTop: theme.spacing(2),
  },
  headerTitle: {
    fontSize: "1.125rem",
  },
  root: {
    padding: 20,
  },
  box: {
    display: "flex",
    alignItems: "center",
  },
}));
